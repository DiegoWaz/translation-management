# ─────────────────────────────────────────────────────────────
# Configuration équipe — seul fichier à personnaliser
# ─────────────────────────────────────────────────────────────
# Installation :
#   cp .env.example .env
#   # renseigner les valeurs ci-dessous
#   pnpm install && pnpm run dev
#
# .env est gitignored — ne jamais committer de token.
# ─────────────────────────────────────────────────────────────

# ─── OAuth GitHub (Sign in with GitHub) ─────────────────────
# Créer une OAuth App : https://github.com/settings/developers
# Callback URL : http://localhost:8443
VITE_GH_CLIENT_ID=
GH_CLIENT_SECRET=

# ─── Config manuelle (optionnel, fallback si pas d'OAuth) ───
VITE_GH_TOKEN=
VITE_GH_OWNER=
VITE_GH_REPO=
VITE_GH_BRANCH=main
VITE_GH_BASE_LANG=fr-FR
VITE_TRANSLATIONS_FOLDER_NAME=translations
VITE_ALWAYS_NEST_JSON=true
VITE_GH_CONFIG_PATH_TEMPLATE=configs/{lang}.json
VITE_GH_CONFIG_SCHEMA_PATH=configs/schema.json
# Locales actives (source de vérité équipe) — séparées par des virgules
VITE_GH_LANGS=en-UK,fr-FR
